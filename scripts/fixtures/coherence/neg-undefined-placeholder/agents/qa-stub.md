# qa-stub agent (neg-undefined-placeholder fixture)

This agent uses <evid> to reference evidence.
The review requires <evid> to be populated before submission.
All findings must include <evid> as supporting material.
